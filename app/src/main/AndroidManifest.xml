<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.cmpt276theta.sudokuvocabulary">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".controller.GameStartActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".controller.WordListActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".controller.LaunchActivity"
            android:theme="@style/FullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.AboutPageActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".controller.SettingsActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".controller.MainMenuActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".controller.GameActivity"
            android:theme="@style/FullScreenTheme" />
    </application>

</manifest>